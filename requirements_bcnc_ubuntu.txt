# bCNC GUI Controller dependencies for Ubuntu/Linux
# Note: PyGetWindow doesn't work on Linux, so we use system tools instead

# Python dependencies
PyAutoGUI>=0.9.54

# System dependencies (install with apt):
# sudo apt install xdotool wmctrl x11-utils
# 
# xdotool - Primary window management tool
# wmctrl - Fallback window management tool  
# x11-utils - Additional X11 utilities (includes xwininfo)

# Optional: bCNC can be installed separately if not using system package
# bCNC>=0.9.16

# For the SVG converter component
opencv-python>=4.8.0
numpy>=1.24.0
scikit-image>=0.21.0
svgwrite>=1.4.0