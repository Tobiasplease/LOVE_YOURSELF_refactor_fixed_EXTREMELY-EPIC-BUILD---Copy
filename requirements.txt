# Core dependencies for LOVE_YOURSELF project
# Computer Vision and Image Processing
opencv-python>=4.8.0
numpy>=1.24.0

# Machine Learning and Object Detection
ultralytics>=8.0.0
torch>=2.0.0
torchvision>=0.15.0

# HTTP Requests and API Communication
requests>=2.31.0

# Serial Communication (for Arduino/servo control)
pyserial>=3.5

# uArm Swift Pro robotic arm control (original uFactory SDK)
git+https://github.com/uArm-Developer/pyuf.git

# Additional utilities that might be needed
Pillow>=10.0.0
matplotlib>=3.7.0

# Hearing (experimental, not in the runtime path — debug/test_hearing.py only).
# Install by hand when testing; left out of the default install so a normal
# setup doesn't pull ctranslate2. CPU int8 on purpose: the VLM owns the GPU.
# faster-whisper>=1.2.0
# sounddevice>=0.4.6

#bCNC>=0.9.16
vpype
vpype-gcode
scikit-image
PyAutoGUI
PyGetWindow
spacy
en_core_web_sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl