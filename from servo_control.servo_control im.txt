from servo_control.servo_control import ServoController

# Init
servos = ServoController(port='COM3')

# In loop
if face_detected:
    pan, tilt = face_coords
    servos.set_pan(pan)
    servos.set_tilt(tilt)

if lonely:
    servos.set_lung("fast")
else:
    servos.set_lung("slow")
im